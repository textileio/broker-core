apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: ipfs-cluster
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: ipfs
          resources:
            requests:
              memory: 1Gi
              cpu: "250m"
            limits:
              memory: 2Gi
              cpu: "500m"


