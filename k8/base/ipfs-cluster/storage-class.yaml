  kind: StorageClass
  apiVersion: storage.k8s.io/v1
  metadata:
    name: premium-ipfs-cluster
  provisioner: pd.csi.storage.gke.io
  volumeBindingMode: WaitForFirstConsumer
  allowVolumeExpansion: true
  parameters:
    type: pd-ssd
---
  kind: StorageClass
  apiVersion: storage.k8s.io/v1
  metadata:
    name: balanced-ipfs-cluster
  provisioner: pd.csi.storage.gke.io
  volumeBindingMode: WaitForFirstConsumer
  allowVolumeExpansion: true
  parameters:
    type: pd-balanced