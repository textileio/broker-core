apiVersion: apps/v1
kind: Deployment
metadata:
  name: broker-01
spec:
  template:
    spec:
      containers:
        - name: broker
          env:
            - name: BROKER_GPUBSUB_PROJECT_ID
              valueFrom:
                secretKeyRef:
                  name: broker-secret
                  key: gpubsub-project-id
            - name: BROKER_GPUBSUB_API_KEY
              valueFrom:
                secretKeyRef:
                  name: broker-secret
                  key: gpubsub-api-key
